services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - '3000:3000'
    volumes:
      - .:/app:cached
      - /app/node_modules
    environment:
      NODE_ENV: development
      NEXT_TELEMETRY_DISABLED: 1
    command: pnpm --filter @hunty/web dev -- --hostname 0.0.0.0 --port 3000
    depends_on:
      - db
    stdin_open: true
    tty: true

  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: hunty
      POSTGRES_PASSWORD: hunty
      POSTGRES_DB: hunty_dev
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
